name: security

on:
  push:
    branches: [main]
  pull_request:
    types: [opened, reopened, synchronize]
    branches-ignore:
      - "**/graphite-base/**"

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  cargo-audit:
    name: Rust dependency audit
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Install cargo-audit
        run: cargo install cargo-audit --locked
      - name: Run cargo audit
        # RUSTSEC-2025-0020: pyo3 buffer overflow — requires major upgrade (0.22→0.24), tracked separately
        run: cargo audit --ignore RUSTSEC-2025-0020

  pip-audit:
    name: Python dependency audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          version: "0.6.2"
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Export requirements from uv.lock
        run: uv export --format requirements-txt --no-hashes --no-dev > /tmp/requirements.txt
      - uses: pypa/gh-action-pip-audit@v1.1.0
        with:
          inputs: "/tmp/requirements.txt"

  dependency-review:
    name: Dependency review
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/dependency-review-action@v4
        with:
          fail-on-severity: high
